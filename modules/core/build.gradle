dependencies {
    api 'org.yaml:snakeyaml:2.2' //Same as in DB
    api 'org.slf4j:slf4j-api:2.0.9' //If change, update log.CommonLoggerService.REQUESTED_API_VERSION
}

repositories {
    mavenCentral()
}
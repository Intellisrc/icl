dependencies {
    api project(':core')
    api "org.reflections:reflections:0.9.12" //Same as in DB
    api 'org.apache.tika:tika-core:2.0.0'
    // We set Jedis to be optional so it is not always required, only when AutoConfig is being used.
    api "com.sleepycat:je:18.3.12"
    api 'redis.clients:jedis:3.6.3'
}